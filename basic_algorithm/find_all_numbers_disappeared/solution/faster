class Solution:
    def findDisappearedNumbers(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        numsSet = set(nums)
        return list(i for i in range(1, len(nums) + 1) if i not in numsSet)
